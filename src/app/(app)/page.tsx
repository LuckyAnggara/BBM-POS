// This file is no longer used for the main dashboard.
// The dashboard content has been moved to /src/app/(app)/dashboard/page.tsx
// You can delete this file if it's no longer needed for any other purpose,
// or repurpose it if you intend for "/" to show different content for logged-in users
// than the public landing page at /src/app/page.tsx.
// For now, we'll leave it empty to avoid conflicts and assume /dashboard is the primary authenticated landing.

export default function AppRootPage() {
  return null; // Or some placeholder if you decide to use this path differently
}
